$("#comments_area").html("<%= j(render 'book_comments/comment_index', book: @books) %>");
$('.book_comments_count').html("<%= j(render 'book_comments/count', book: @comment.book) %>")
$("textarea").val('');